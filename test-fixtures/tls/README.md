# TLS test fixtures

Run `npm run fixtures:tls` to generate a local CA, client certificate, and password-protected `client.p12` under `test-fixtures/tls/generated/`.

The generated files are test-only. The known password is `poo-pi-test-password` and exists only for smoke checks.
